class Solution:
    def findMedian(self, arr):
        n = len(arr)
        arr.sort()
        
        if n % 2 != 0:
            return float(arr[n // 2])
        else:
            mid1 = arr[n // 2 - 1]
            mid2 = arr[n // 2]
            return (mid1 + mid2) / 2
